import { createFileRoute } from "@tanstack/react-router";

function NilusChangesRouteView() {
  return null;
}

export const Route = createFileRoute("/nilus/changes")({
  component: NilusChangesRouteView,
});
