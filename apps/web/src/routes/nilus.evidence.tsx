import { createFileRoute } from "@tanstack/react-router";

function NilusEvidenceRouteView() {
  return null;
}

export const Route = createFileRoute("/nilus/evidence")({
  component: NilusEvidenceRouteView,
});
