import { createFileRoute } from "@tanstack/react-router";

function NilusTasksRouteView() {
  return null;
}

export const Route = createFileRoute("/nilus/tasks")({
  component: NilusTasksRouteView,
});
